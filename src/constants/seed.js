const seedRows = (cb) => {
    cb([
        {
            type: 'input',
            host: 'Lambents-MBP:~ lambentowl$',
            body: '',
            disabled: false,
        },
    ]);
};

export default seedRows;
