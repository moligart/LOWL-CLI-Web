const seedRows = (cb) => {
    cb([
        {
            type: 'input',
            host: 'LambentOwl:~ $',
            body: '',
            disabled: false,
        },
    ]);
};

export default seedRows;
